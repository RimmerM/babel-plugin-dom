{
  "name": "babel-plugin-dom",
  "version": "0.0.1",
  "description": "Babel JSX transformer for the DOM library",
  "main": "src/transform.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rimmer",
  "dependencies": {
    "babel-plugin-syntax-jsx": "^6.18.0"
  }
}
